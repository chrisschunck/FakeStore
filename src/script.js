import Carrinho from "Carrinho.jsx"

const ProductCard = document.elementById("ProductCard");
const buttonOnClick = document.elementById("buttonOnClick");

export default function funcaoButtonOnClick(funcaoCart, funcaoVoltar) {
  const [ProductCard, setProdutoCard] = useState([])
  const [buttonOnClick, setButtonOnClick] = useState([])
  
  return (
    function funcaoCart() {
      const card = element.addEventListener("ProductCard");
}
    
    function funcaoVoltar() {
    const buttonOnClick = element.addEventListener("buttonOnClick");
}
  )
}
