const e-mail = document.getElementById("e-mail");
const senha = document.getElementById("senha");
const register = document.getElementById("register");
const login = document.getElementById("login");
const logout = document.getElementById("logout");

function login(e-mail, senha) {
  const login = document.getElementById("e-mail", "senha");
  document.getLocalStorage(e-mail, senha);
  document.setLocalStorage(login);
}

function logout(login) {
  const logout = document.getElementById("login").remove;
  document.setLocalStorage(logout);
}

function register(nome, e-mail, senha) {
  const register = document.getElementById("nome", "e-mail", "senha");
  document.getLocalStorage("nome", "e-mail", "senha");
  const saved = document.setLocalStorage(register);
}
